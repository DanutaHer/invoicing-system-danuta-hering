server:
  port: 9999

invoicing-system:
  database:
    directory: db
    type: file

    file:
      id: id.txt
      invoices: invoices.txt

spring:
  profiles:
    active: prod
  flyway:
    enabled: false