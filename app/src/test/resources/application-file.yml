invoicing-system:
  database:
    directory: db
    type: file
    file:
      companyId: companyId.txt
      invoiceId: invoiceId.txt
      invoices: invoices.txt
      companies: companies.txt

spring:
  profiles:
    active: file
  flyway:
    enabled: false