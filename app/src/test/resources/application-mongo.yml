invoicing-system:
  database:
    type: mongo
    name: invoices
    collection:
      invoices: invoices
      companies: companies
    counter:
      collection: counter

spring:
  profiles:
    active: mongo
  flyway:
    enabled: false